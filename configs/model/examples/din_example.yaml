# EXAMPLE ONLY: lightweight scaffold inspired by Deep Interest Network (arXiv:1706.06978).
# Uses target-aware attention and an example auxiliary head.
_target_: models.paper_example_models.DINExample
embed_dim: 32
num_fields: 16
hidden_dims: [128, 64]
dropout: 0.1
use_auxiliary_head: true
num_classes: 1
task_type: binary_classification
